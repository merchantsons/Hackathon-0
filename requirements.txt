# AI Employee — Bronze Tier Dependencies
# Install: pip install -r requirements.txt
#
# Python >= 3.10 required (uses match/case-compatible typing)
# Tested on Python 3.11 and 3.12

# ── Core dependencies ────────────────────────────────────────
# Filesystem event monitoring for the Vault Watcher
watchdog>=4.0.0

# ── Standard library modules used (no install needed) ────────
# os, sys, shutil, hashlib, logging, signal, json, argparse
# pathlib, datetime, time, typing — all stdlib

# ── Silver+ tier dependencies (not needed for Bronze) ────────
# anthropic>=0.25.0        # Claude API (Silver: LLM reasoning)
# mcp>=1.0.0               # Model Context Protocol (Silver: MCP servers)
# httpx>=0.27.0            # HTTP client (Silver: external APIs)
python-dotenv>=1.0.0       # .env file loading (DRY_RUN, etc.)
# schedule>=1.2.0          # Task scheduling (Gold: Ralph Wiggum loop)
